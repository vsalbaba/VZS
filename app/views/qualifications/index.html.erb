<% title t('label.qualification.index') %>
<% context_menu do %>
    <%= link_new new_qualification_path, t('label.qualification.new') %>
<% end %>

<table class="table table-striped">
    <thead>
    <tr>
        <th>Zkratka</th>
        <th>Název</th>
        <th><%= t("label.actions") %></th>
    </tr>
    </thead>
    <tbody>
    <% @qualifications.each do |qualification| %>
        <tr>
            <td><%= link_to qualification.abbreviation, qualification %></td>
            <td><abbr title="<%= qualification.description%>"><%= qualification.name %></abbr></td>
            <td>
                <div class="btn-group">
                    <%= link_to t("btn.edit"), edit_qualification_path(qualification), :class => 'btn btn-mini' %>
                </div>
            </td>
        </tr>
    <% end %>
    </tbody>
</table>
