<%= semantic_form_for @qualification do |f| %>
    <%= f.semantic_errors %>
    <%= f.inputs %>
    <%= f.actions do %>
        <%= f.action :submit, :as => :button, :label => t('btn.save'), :button_html => {:class => 'btn btn-success'} %>
    <% end %>
<% end %>