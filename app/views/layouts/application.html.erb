<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "VZS" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <!-- Le styles -->
    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- Le fav and touch icons -->
    <!--
    <link href="images/favicon.ico" rel="shortcut icon">
    <link href="images/apple-touch-icon.png" rel="apple-touch-icon">
    <link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
    <link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
    -->
  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <%= link_to 'VZS ČČK Třebíč', root_path, :class => 'brand' %>
          <div class="container-fluid nav-collapse">
            <%= render 'layouts/navbar' %>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container">
      <div class="row"> <!-- main header - logo + napis -->

        <div class="row vzs-header">
          <div class="span2"> <!-- Logo -->
            <br />
              <%= link_to root_path, :class => 'vzs-main-logo-container' do %>
                <%= image_tag("logo_vzs.gif", :size => "150x150", :class => "vzs-main-logo img-circle") %>
              <% end %>
          </div>
          <div class="span10"> <!-- Nadpis -->
            <%= render 'layouts/title' %>
          </div>
        </div>


        <div class="row vzs-main-content">
          <div class="row">
            <div class="span3" id="vzs-left-menu"> <!-- Menu -->
              <div class="well sidebar-nav">
                 <%= render 'layouts/menu' %>
              </div><!--/.well -->
            </div><!--/span-->

            <div class="span8 offset1" id="vzs-main-content">
              <% if content_for? :context_menu %>
              <div class="row">
                  <div class="pull-right">
                    <div class="btn-group">
                      <%= yield(:context_menu) %>
                    </div>
                  </div>
                </div>
                <% end %>

              <% flash.each do |name, msg| %>
                <%= content_tag :div, msg, :id => "flash_#{name}", :class => "alert alert-#{name == :notice ? 'success' : 'error'} " %>
              <% end %>

              <div class="row" id="vzs-page-content">
                <%= yield %>
              </div>
            </div>
          </div>
        </div><!--/row vzs-main-content-->
      </div><!-- main header - logo + napis -->

      <hr />
      <footer>
      <p>&copy; FriendlySystems.cz 2012</p>
      </footer>
    </div> <!-- /container -->

    <!-- Le javascript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
