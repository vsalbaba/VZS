<!DOCTYPE html>
<html lang="en">
  <head>
    <meta name="google-site-verification" content="qC7Sivtmu4FlgBtGPo6LLbwdjEy7VIOUfrNswtO2QWM" />
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="MS VZS ČČK Třebíč je občanské sdružení jehož cílem je zajišťovat bezpečnost u vody, vodácké akce a jiné sportovní akce. Dále zajišťujeme přednášky a ukázky první předlékařské pomoci." />
    <%= auto_discovery_link_tag :atom, "/feed" %>
    <%= auto_discovery_link_tag :rss, "/feed.rss" %>

    <title><%= content_for?(:title) ? yield(:title) : "VZS Třebíč" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <!-- Le styles -->
    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- Le fav and touch icons -->
    <%= favicon_link_tag %>
    <!--
    <link href="images/favicon.ico" rel="shortcut icon">
    <link href="images/apple-touch-icon.png" rel="apple-touch-icon">
    <link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
    <link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
    -->
  </head>
  <body>
<div id="nav" class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
      <%= link_to 'VZS ČČK Třebíč', root_path, :class => 'brand' %>
            <div class="nav-collapse">
              <%= render "layouts/navbar"%>
            </div>
            <!--/.nav-collapse -->
        </div>
    </div>
</div>

    <div class="container vzs-main-page">
      <div class="row"> <!-- main header - logo + napis -->
        <div class="row vzs-header">
          <div class="span8 offset2" id="vzs-main-title"> <!-- Nadpis -->
            <%= render 'layouts/title' %>
          </div>
        </div>


        <div class="row vzs-main-content">
          <div class="row">
            <div class="span3" id="vzs-left-menu"> <!-- Menu -->
              <div class="sidebar-nav">
                  <%= link_to root_path, :class => 'vzs-main-logo-container' do %>
                    <%= image_tag("znakVZS_150x150.gif", :size => "150x150", :class => "vzs-main-logo img-circle") %>
                  <% end %>
                 <%= render 'layouts/menu' %>
                 <%= render 'layouts/treninky' %>


               <%= link_to 'http://www.cez.cz' do %>
                <%= image_tag("logo-skupina-cez.jpg", :class => "banner") %>
              <% end %>

              <%= link_to 'http://www.zpravodajedu.cz' do %>
                <%= image_tag("dukovany_160x160_v02.gif", :class => "banner") %>
              <% end %>

              </div><!--/.well -->
            </div><!--/span-->

            <div class="span9"  id="vzs-main-content">
              <% if content_for? :context_menu %>
              <div class="row">
                  <div class="pull-right">
                    <div class="btn-group">
                      <%= yield(:context_menu) %>
                    </div>
                  </div>
                </div>
                <% end %>

              <% flash.each do |name, msg| %>
                <%= content_tag :div, msg, :id => "flash_#{name}", :class => "alert alert-#{name == :notice ? 'success' : 'error'} " %>
              <% end %>

              <div class="row" id="vzs-page-content">
                <div class="span9">
                  <%= yield %>
                </div>
              </div>
            </div>
          </div>
        </div><!--/row vzs-main-content-->
      </div><!-- main header - logo + napis -->

      <hr />
      <footer>
      <p>
      &copy;
      FriendlySystems.cz 2012, verze <a class="#" class="tooltip" title="<%= REVISION%>"><%= CURRENT_VERSION%></a>
        <span class="pull-right">
      <% if current_user %>
        <%= link_to t('btn.edit_profile'), edit_user_path(current_user) %> |
        <%= link_to t('btn.logout'), logout_path %>
      <% else %>
      <%= link_to t('btn.login'), login_path %>
    <% end %>
      </span>
      </p>
      </footer>
    </div> <!-- /container -->

    <!-- Le javascript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>
    <%= render "layouts/google_analytics" %>
  </body>
</html>
