<%= semantic_form_for [@comment.article, @comment] do |f| %>
	<%= f.semantic_errors %>
	<%= f.inputs do %>
    <%= f.input :message, :label => 'Váš komentář:' %>
  <% end %>
  <%= f.actions do %>
    <%= f.action :submit, :as => :button, :button_html => { :class => 'btn btn-success' } %>
  <% end %>
<% end %>
