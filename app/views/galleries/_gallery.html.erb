<div id="carousel" class="es-carousel-wrapper">
    <div class="es-carousel">
        <ul>
          <% gallery.photos.each do |photo| %>
            <li>
              <a href="<%= photo.image.url%>", rel="lightbox[gallery-<%= gallery.to_param%>]">
                <%= image_tag photo.image.url(:medium) %>
              </a>
            </li>
          <% end %>
        </ul>
    </div>
</div>
