<%= content_tag_for :li, participant do %>
  <% if no_can_do then %>
    <del><%= participant.full_name %></del>
  <% else %>
    <%= participant.full_name %>
  <% end %>
  <% if can? :unparticipate, Event %>
    <%= link_to unparticipate_event_path(event, :participant_id => participant.id), :method => :put do %>
      <%= glyph :remove %>
    <% end %>
  <% end %>
<% end %>
