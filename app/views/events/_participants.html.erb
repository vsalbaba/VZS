<% if can? :see_participants, event %>
  <ol>
    <% event.participants.each do |participant| %>
      <%= render "participant", :participant => participant, :event => event %>
    <% end %>
  </ol>
<% end %>
