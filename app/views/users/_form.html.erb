<%= semantic_form_for @user do |f| %>
	<%= f.inputs do %>
		<%= f.input :login, :label => 'Přihlašovací jméno' %>
		<%= f.input :password, :label => 'Heslo' %>
		<%= f.input :password_confirmation, :label => 'Potvrzení hesla' %>
	<% end %>
	<%= f.buttons do %>
		<%= f.commit_button :label => @user.id.nil? ? 'Registrovat' : 'Uložit'  %>
	<% end %>
<% end %>
