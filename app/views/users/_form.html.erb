<%= semantic_form_for @user do |f| %>
	<%= f.inputs do %>
      <%= f.input :login, :label => t('user.attr.login') %>
      <%= f.input :password, :label => t('user.attr.password'), :input_html => { :autocomplete => 'off' } %>
      <%= f.input :password_confirmation, :label => t('user.attr.password_confirmation'), :input_html => { :autocomplete => 'off' } %>
        <% if can? :manage, User %>
          <%= f.input :group, :as => :select, :collection => group_names, :label => 'Skupina' %>
        <% end %>
    <%= render 'profiles/form', :f=>f %>
  <% end %>
	<%= f.buttons do %>
      <%= f.action :submit, :as => :button,
        :label => @user.id.nil? ? t('btn.register') : t('btn.save'),
        :button_html => { :class => 'btn btn-success' }   %>
	<% end %>
<% end %>
